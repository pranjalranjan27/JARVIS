package com.jarvis.engine;

/**
 * ResponseGenerator — formerly held hardcoded personality templates.
 * Now gutted: all conversational responses are handled by the LLM layer (OllamaService).
 * Kept as a shell for backward compatibility.
 */
// Sarcastic personality mode response generation
public class ResponseGenerator {
// Companion mode supportive response behavior
    public ResponseGenerator() {
        // No-op — templates removed, LLM handles responses now
    }
}
